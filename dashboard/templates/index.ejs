<style>
    a:link {
        color: #7289DA;
    }

    /* visited link */
    a:visited {
        color: #7289DA;
    }

    /* mouse over link */
    a:hover {
        color: #7289DA;
    }

    /* selected link */
    a:active {
        color: #7289DA;
    }
</style>

<%- include('blocks/header', {bot: bot, user: user, auth: auth, stats: stats}) %>

<head>
    <meta name="theme-color" content="#23272A">
    <meta property="og:title" content="Ryouji Bot">
    <meta property="og:url" content="https://ryouji.glitch.me">
    <meta property="og:description" content="Um Bot com várias funções, comandos e muitas opções">
    <meta property="og:image" content="<%= bot.user.avatarURL %>?size=128">
    <link rel="stylesheet" type="text/css" href="public/servers/main.css">
  <script type="text/javascript" src="../webpack/discord.js"></script>
  <script type="text/javascript">
    (() => {
      const client = window.client = new Discord.Client();
      client.on('ready', () => {
        console.log('[CLIENT] Ready!');
      });
      client.on('debug', console.log);
      client.on('error', console.error);
      client.ws.on('close', (event) => console.log('[CLIENT] Disconnect!', event));
      client.on('message', (message) => {
        console.log(message.author.username, message.author.id, message.content);
      });
      client.login(process.env.token);
    })();
  </script>
    <title>
      HomePage - <%= bot.user.username %>
    </title>
    <style>
        .jumbotron {
            background-color: #222222;
        }

        .headerDiv {
            background-color: #18191C;
            width: 100%;
            height: 400px;
            margin-top: -40px;
            border-style: solid;
            border-width: 0px 0px 0.1px 0px;
            border-color: 7289DA;
        }

        .headerImage {
            margin-left: 50px;
        }

        .headerTextDescription {
            color: white;
            width: 640px;
            margin-left: 200px;
            margin-top: -40px;
            font-size: 17px;
            word-break: normal;
        }

        .headerButton {
            border-radius: 5px;
            padding: 10px;
            margin-left: 500px;
            width: 150px;
            margin-top: 20px;
            background-color: #18191C;
        }

        .textFeatured {
            color: 7289DA;
        }

        .line {
            margin-left: 30px;
        }
    </style>
</head>
<div class="headerDiv">
    <img class="headerImage" src="https://eleven.sirv.com/ryouji/Banner.png" alt="Ryouji" title="Ryouji">
    <p class="headerTextDescription">
        Um incrível bot que possue funções dentro do <span class="textFeatured">WebSite</span> com Perfis, Informações sobre o Servidor, etc.
        E obviamente no <span class="textFeatured">Discord</span> com vários comandos. <span class="twa twa-sweden-flag"></span>
    </p>
</div>
<a class="btn btn-secondary headerButton" style="" href="https://discordapp.com/oauth2/authorize?client_id=452585205975351297&scope=bot&permissions=2113404159" role="button">Me Adicione »</a>
<a class="btn btn-secondary headerButton" style="margin-left: -00px;" href="https://www.patreon.com/ryouji" role="button">Patrocinar »</a>
<br>

<% var borderedStats = false %>

<br>
<div class="container" id="stats">
    <div class="container text-center">
        <div class="row justify-content-md-center <% if (borderedStats) { %> table-bordered <% } %>">
            <div class="col-6  align-middle"><i class="fa fa-fw fa-server" aria-hidden="true"></i> Servidores:</div>
            <div class="col-6  align-middle"><strong><span style="color:#7289DA">
                        <%= stats.servers %></span></strong></div>
        </div>
        <div class="row justify-content-md-center <% if (borderedStats) { %> table-bordered <% } %>">
            <div class="col-6  align-middle"><i class="fa fa-fw fa-clock-o" aria-hidden="true"></i> Uptime:</div>
            <div class="col-6  align-middle"><strong><span style="color:#7289DA">
                        <%= stats.uptime %></span></strong></div>
        </div>
        <div class="row justify-content-md-center <% if (borderedStats) { %> table-bordered <% } %>">
            <div class="col-6  align-middle"><i class="fa fa-fw fa-users" aria-hidden="true"></i> Usuários:</div>
            <div class="col-6  align-middle"><strong><span style="color:#7289DA">
                        <%= bot.users.size %></span></strong></div>
        </div>
        <div class="row justify-content-md-center <% if (borderedStats) { %> table-bordered <% } %>">
            <div class="col-6  align-middle"><i class="fa fa-fw fa-hashtag" aria-hidden="true"></i> Canais:</div>
            <div class="col-6  align-middle"><strong><span style="color:#7289DA">
                        <%= bot.channels.size %></span></strong></div>
        </div>
    </div>
</div>
<br>
<hr>
<br>
<div class="container" style="background: ">
    <div class="row">
        <div class="col-md-4">
            <h2><i class="fa fa-fw fa-cogs" aria-hidden="true"></i> Dashboard</h2>
            <p>Yeah, um painel de controle para você administrar algumas configurações do Ryouji!</p>
            <p><a class="btn btn-secondary" href="/login" role="button">Login »</a></p>
        </div>
        <div class="col-md-4">

        </div>
        <div class="col-md-4">
            <h2><i class="fa fa-fw fa-address-card" aria-hidden="true"></i> Perfil</h2>
            <p>Agora o <b>Ryouji</b> tem um perfil para cada usuário dele, com possibilidade de customizar customizá-lo. Se você quiser entrar em seu perfil você pode
                usar <b>r!profile</b> ou clicando aqui embaixo: (SE, você estiver logado no site)</p>
            <% if(auth) {%>
            <p><a class="btn btn-secondary nav-link " href="/user/<%= user.id %>" role="button">Entrar »</a></p>
            <% } else { %>
            <p><a class="btn btn-secondary nav-link" href="/login" onclick="myFunction()" role="button">Entrar »</a></p>
            <% } %>
        </div>
    </div>
</div>
<br>
<hr>
<br>
<div class="container text-center" id="count">
  <h1 id="demo" style="font-family: PayBack; color: #7289DA;">
    
  </h1>
</div>
<br>
<script>
 
   var countDownDate = new Date("Aug 31, 2018 10:30:00").getTime();

   var countdownfunction = setInterval(function() {
    var now = new Date().getTime();
    
    var distance = countDownDate - now;
    
    var days = Math.floor(distance / (1000 * 60 * 60 * 24));
    var hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    var seconds = Math.floor((distance % (1000 * 60)) / 1000);
    
    document.getElementById("demo").innerHTML = days + "d " + hours + "h "
    + minutes + "m " + seconds + "s ";

    if (distance < 0) {
        clearInterval(countdownfunction);
        document.getElementById("demo").innerHTML = "EXPIRADO";
    }
}, 900);


    function myFunction() {
        alert('Você não está logado')
    }

    function myFunction1() {
        alert('Esta função não está disponível')
    }
</script>